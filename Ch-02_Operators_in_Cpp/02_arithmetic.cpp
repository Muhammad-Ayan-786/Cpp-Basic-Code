#include<iostream>
using namespace std;

int main() {
    int a = 11, b = 5;

    cout << "Sum = " << (a + b) << endl;
    cout << "Difference = " << (a - b) << endl;
    cout << "Product = " << (a * b) << endl;
    cout << "Division = " << (a / b) << endl;
    cout << "Modulo = " << (a % b) << endl;


    // int sum = a + b;
    // cout << "Sum = " << sum << endl;

    return 0;
}